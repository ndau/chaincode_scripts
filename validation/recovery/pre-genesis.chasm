; Pre-genesis recoverable account
;
; The account is initialized with two validation keys, generated by the recovery
; service. Any one key is needed to sign a ChangeValidation transaction, and no other
; transactions are permitted.
;
; Expected arguments on the stack in order from bottom to top are:
;    account
;    transaction
;    bitmask of which authorized keys were found in the signature list

handler EVENT_DEFAULT {
    fail
}

handler EVENT_CHANGEVALIDATION {
    zero                            ; if we get here at all there's at least one signature
}
