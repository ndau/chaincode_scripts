; Register Node (become an active node)
;
; Checks to see that the node account's total stake (including co-stake) is greater than
; the minimum specified by the MinNodeRegistrationStakeAmount system variable.
;
; NEEDS TO BE CORRECTED TO MATCH THAT DESCRIPTION

handler EVENT_DEFAULT {
    zero
}