DISBURSEMENT_QTY_FIELD_ID = 10

; stack top: list of costaker account data objects
handler EVENT_DEFAULT {
    deco zro DISBURSEMENT_QTY_FIELD_ID
}

; called via deco, therefore needs no args: it always gets a list entry
func zro(0) {
    zero
}
